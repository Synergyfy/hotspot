{
  "name": "hotspot-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:generate": "pnpm --filter @hotspot/api db:generate",
    "db:push": "pnpm --filter @hotspot/api db:push",
    "db:migrate": "pnpm --filter @hotspot/api db:migrate"
  },
  "devDependencies": {
    "turbo": "^2.5.5"
  },
  "packageManager": "pnpm@9.15.0"
}
